name: Action on tag

on:
  push:
    tags:
      - "**.*.*"
    branches: on-tag

jobs:
  test-tagging:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.4
      - name: After Checkout
        env:
          TAG: ${{ github.ref | cut -d / -f 3 }}
        run: |
          cat src/version.js
          echo "$TAG"
          sed -i "s/LP_REACT: '*.*.*'/LP_REACT: '$TAG'/" src/version.js
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git commit -m "commit from action"
          git push origin on-tag
