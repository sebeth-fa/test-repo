name: Action on tag

on:
  push:
    tags:
      - "**.*.*"
    branches: on-tag

jobs:
  test-tagging:
    runs-on: ubuntu-latest
    steps:
      - name: Got Tagged
        env:
          GITHUB_CONTEXT: ${{ github.ref }}
        run: |
          echo "$GITHUB_CONTEXT"
